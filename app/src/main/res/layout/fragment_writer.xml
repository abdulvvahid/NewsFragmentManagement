<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:context=".WriterFragment">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tv_writer_name1"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="Valencia"
                app:drawableTopCompat="@drawable/ic_launcher_foreground" />

            <TextView
                android:id="@+id/tv_writer_name2"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="Valencia"
                app:drawableTopCompat="@drawable/ic_launcher_foreground" />

            <TextView
                android:id="@+id/tv_writer_name3"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="Valencia"
                app:drawableTopCompat="@drawable/ic_launcher_foreground" />

            <TextView
                android:id="@+id/tv_writer_name4"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="Valencia"
                app:drawableTopCompat="@drawable/ic_launcher_foreground" />

            <TextView
                android:id="@+id/tv_writer_name5"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="Valencia"
                app:drawableTopCompat="@drawable/ic_launcher_foreground" />

            <TextView
                android:id="@+id/tv_writer_name6"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="Valencia"
                app:drawableTopCompat="@drawable/ic_launcher_foreground" />

        </LinearLayout>

    </HorizontalScrollView>
</layout>